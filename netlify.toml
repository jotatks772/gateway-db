# Configurações de build para a Netlify
[build]
  command = "npm run build"
  publish = "dist"

# Regra de reescrita para Single Page Applications (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
